<Window x:Class="Tracker.Help.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title, FallbackValue='Tracker Help'}"
        Height="700" Width="1000"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

            <!-- TreeView Style -->
            <Style x:Key="TocTreeViewItem" TargetType="TreeViewItem">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="Margin" Value="0,1"/>
                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TreeViewItem">
                            <StackPanel>
                                <Border x:Name="Bd" Padding="{TemplateBinding Padding}" 
                                        Background="Transparent" CornerRadius="4" Margin="2,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <ToggleButton x:Name="Expander" Grid.Column="0" 
                                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ClickMode="Press" Width="16" Visibility="Collapsed">
                                            <ToggleButton.Style>
                                                <Style TargetType="ToggleButton">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border Background="Transparent" Padding="2">
                                                                    <Path x:Name="Arrow" Fill="{DynamicResource ForegroundBrush}" 
                                                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                          Data="M 0 0 L 6 4 L 0 8 Z"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsChecked" Value="True">
                                                                        <Setter TargetName="Arrow" Property="Data" Value="M 0 0 L 8 0 L 4 6 Z"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>
                                        <TextBlock Grid.Column="1" Text="{Binding Icon}" Margin="0,0,8,0" FontSize="14"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Title}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                                <ItemsPresenter x:Name="ItemsHost" Margin="16,0,0,0"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="HasItems" Value="True">
                                    <Setter TargetName="Expander" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsExpanded" Value="False">
                                    <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SelectedBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Button Style -->
            <Style x:Key="HelpNavButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                    CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Search Box Style -->
            <Style x:Key="SearchBox" TargetType="TextBox">
                <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                    <TextBlock x:Name="Placeholder" Text="Search help..." 
                                               Foreground="#666666" Margin="{TemplateBinding Padding}"
                                               Visibility="Collapsed" IsHitTestVisible="False"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" 
            Background="{DynamicResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource PopupBackgroundBrush}" 
                    CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icon and Title -->
                    <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="12,0">
                        <TextBlock Text="📖" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="Tracker Help" Margin="8,0,0,0" FontWeight="SemiBold" 
                                   FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,8,0">
                        <Button Style="{StaticResource HelpNavButton}" Click="Minimize_Click" ToolTip="Minimize">
                            <TextBlock Text="─" FontSize="12"/>
                        </Button>
                        <Button Style="{StaticResource HelpNavButton}" Click="Maximize_Click" ToolTip="Maximize">
                            <TextBlock Text="□" FontSize="12"/>
                        </Button>
                        <Button Style="{StaticResource HelpNavButton}" Click="Close_Click" ToolTip="Close">
                            <TextBlock Text="✕" FontSize="12"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260" MinWidth="200" MaxWidth="400"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Sidebar - TOC -->
                <Border Grid.Column="0" Background="{DynamicResource PopupBackgroundBrush}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Search Box -->
                        <Grid Grid.Row="0" Margin="12">
                            <TextBox x:Name="SearchBox" Style="{StaticResource SearchBox}"
                                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                     KeyDown="SearchBox_KeyDown"/>
                            <Button Style="{StaticResource HelpNavButton}" 
                                    HorizontalAlignment="Right" Margin="0,0,4,0"
                                    Command="{Binding ClearSearchCommand}"
                                    Visibility="{Binding HasSearchQuery, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="✕" FontSize="10"/>
                            </Button>
                        </Grid>

                        <!-- Search Results -->
                        <ListBox Grid.Row="1" ItemsSource="{Binding SearchResults}"
                                 Visibility="{Binding ShowSearchResults, Converter={StaticResource BoolToVisibilityConverter}}"
                                 Background="Transparent" BorderThickness="0"
                                 MaxHeight="200" Margin="12,0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource ButtonHoverBackgroundBrush}" CornerRadius="4" 
                                            Padding="10,8" Margin="0,2" Cursor="Hand"
                                            MouseLeftButtonDown="SearchResult_Click">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" 
                                                       Foreground="{DynamicResource AccentBrush}"/>
                                            <TextBlock Text="{Binding Snippet}" TextWrapping="Wrap"
                                                       Foreground="{DynamicResource ForegroundBrush}"
                                                       FontSize="11" Opacity="0.8" MaxHeight="40"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Table of Contents -->
                        <TreeView Grid.Row="2" ItemsSource="{Binding TocEntries}"
                                  SelectedItemChanged="TocTree_SelectedItemChanged"
                                  Background="Transparent" BorderThickness="0"
                                  Padding="8" ItemContainerStyle="{StaticResource TocTreeViewItem}">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Icon}" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Title}"/>
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </Grid>
                </Border>

                <!-- Splitter -->
                <GridSplitter Grid.Column="1" Width="1" Background="{DynamicResource BorderBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

                <!-- Right Content Area -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Navigation Bar -->
                    <Border Grid.Row="0" Background="{DynamicResource PopupBackgroundBrush}" 
                            Padding="12,8" BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Navigation Buttons -->
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <Button Style="{StaticResource HelpNavButton}" 
                                        Command="{Binding BackCommand}" ToolTip="Back">
                                    <TextBlock Text="←" FontSize="14"/>
                                </Button>
                                <Button Style="{StaticResource HelpNavButton}" 
                                        Command="{Binding ForwardCommand}" ToolTip="Forward">
                                    <TextBlock Text="→" FontSize="14"/>
                                </Button>
                                <Button Style="{StaticResource HelpNavButton}" 
                                        Command="{Binding HomeCommand}" ToolTip="Home" Margin="8,0,0,0">
                                    <TextBlock Text="🏠" FontSize="14"/>
                                </Button>
                            </StackPanel>

                            <!-- Breadcrumb -->
                            <TextBlock Grid.Column="1" Text="{Binding Breadcrumb}" 
                                       VerticalAlignment="Center" Margin="16,0,0,0"
                                       Foreground="{DynamicResource ForegroundBrush}" FontSize="12"/>

                            <!-- Zoom Controls -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Style="{StaticResource HelpNavButton}" 
                                        Command="{Binding ZoomOutCommand}" 
                                        ToolTip="Zoom Out (Ctrl+-)">
                                    <TextBlock Text="−" FontSize="14" FontWeight="Bold"/>
                                </Button>
                                <TextBlock Text="{Binding ZoomDisplayText}" 
                                           VerticalAlignment="Center" 
                                           Width="45" TextAlignment="Center"
                                           Foreground="{DynamicResource ForegroundBrush}" 
                                           FontSize="12"/>
                                <Button Style="{StaticResource HelpNavButton}" 
                                        Command="{Binding ZoomInCommand}" 
                                        ToolTip="Zoom In (Ctrl++)">
                                    <TextBlock Text="+" FontSize="14" FontWeight="Bold"/>
                                </Button>
                                <Button Style="{StaticResource HelpNavButton}" 
                                        Command="{Binding ZoomResetCommand}" 
                                        ToolTip="Reset Zoom (Ctrl+0)"
                                        Margin="4,0,0,0">
                                    <TextBlock Text="⟲" FontSize="12"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Content -->
                    <FlowDocumentScrollViewer Grid.Row="1" Document="{Binding Document}"
                                              Background="{DynamicResource BackgroundBrush}"
                                              Foreground="{DynamicResource ForegroundBrush}"
                                              VerticalScrollBarVisibility="Auto"
                                              IsToolBarVisible="False"
                                              Zoom="{Binding ZoomLevel, Mode=TwoWay}"
                                              MinZoom="50" MaxZoom="200"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>

