<UserControl x:Class="Tracker.Controls.Analytics.RecommendationsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="400"
             x:Name="RecommendationsPanelControl">

    <UserControl.Resources>
        <!-- Urgency Colors -->
        <SolidColorBrush x:Key="CriticalBrush" Color="#DC3545"/>
        <SolidColorBrush x:Key="HighBrush" Color="#FD7E14"/>
        <SolidColorBrush x:Key="MediumBrush" Color="#FFC107"/>
        <SolidColorBrush x:Key="LowBrush" Color="#28A745"/>
        <SolidColorBrush x:Key="InfoBrush" Color="#17A2B8"/>

        <!-- Recommendation Card Style -->
        <Style x:Key="RecommendationCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Action Step Style -->
        <Style x:Key="ActionStepStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Expander Style for Action Steps -->
        <Style x:Key="ActionExpanderStyle" TargetType="Expander">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Recommendations" 
                       FontSize="18" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryText}"/>
            <TextBlock x:Name="SummaryText"
                       Text="Loading..."
                       FontSize="12" TextWrapping="Wrap"
                       Foreground="{DynamicResource SecondaryText}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Summary Badges -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12"
                    x:Name="BadgesPanel">
            <!-- Badges added programmatically -->
        </StackPanel>

        <!-- Recommendations List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="RecommendationsList">
                <!-- Placeholder for empty state -->
                <TextBlock x:Name="EmptyStateText"
                           Text="No recommendations at this time"
                           FontSize="13"
                           Foreground="{DynamicResource SecondaryText}"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
