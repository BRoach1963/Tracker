<UserControl x:Class="Tracker.Controls.CustomControls.SparklineControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="80"
             x:Name="Root"
             ToolTip="{Binding TooltipText, ElementName=Root}">
    
    <Canvas x:Name="SparklineCanvas"
            Width="{Binding ChartWidth, ElementName=Root}"
            Height="{Binding ChartHeight, ElementName=Root}"
            ClipToBounds="True">
        <!-- Sparkline will be drawn programmatically -->
    </Canvas>
</UserControl>
