<UserControl x:Class="Tracker.Controls.CustomControls.CalendarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="36" d:DesignWidth="36">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Button x:Name="CalendarIconButton"
                Width="36" Height="36"
                ToolTip="Select date"
                Click="CalendarButton_Click"
                Style="{StaticResource QuickActionButton}">
            <Path Data="M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3H18V1M17,12H12V17H17V12Z"
                  Width="18" Height="18"
                  Stretch="Uniform"
                  Fill="{DynamicResource AccentBrush}"/>
        </Button>
        
        <Popup x:Name="CalendarPopup"
               Placement="Bottom"
               StaysOpen="False"
               AllowsTransparency="True">
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="0,4,0,0">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.2" Color="Black"/>
                </Border.Effect>
                <Calendar x:Name="DateCalendar"
                          SelectionMode="SingleDate"
                          SelectedDatesChanged="Calendar_SelectedDatesChanged"
                          Background="{DynamicResource SurfaceBrush}"
                          Foreground="{DynamicResource ForegroundBrush}"
                          BorderThickness="0"/>
            </Border>
        </Popup>
    </Grid>
</UserControl>
