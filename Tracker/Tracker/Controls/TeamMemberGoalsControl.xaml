<UserControl x:Class="Tracker.Controls.TeamMemberGoalsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:Tracker.Common.Converters"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/DialogStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource AccentBrush}" 
                CornerRadius="6,6,0,0"
                Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Goals" 
                           FontWeight="SemiBold" 
                           FontSize="14"
                           Foreground="{DynamicResource BackgroundBrush}"/>
                <TextBlock Grid.Column="1" 
                           Text="{Binding GoalCount, StringFormat='{}{0} goals'}"
                           FontSize="12"
                           Foreground="{DynamicResource BackgroundBrush}"
                           Opacity="0.8"
                           Margin="0,0,12,0"/>
                <Button Grid.Column="2"
                        Content="+ Add"
                        Style="{StaticResource DialogSecondaryButtonStyle}"
                        Command="{Binding AddGoalCommand}"
                        Padding="8,4"
                        FontSize="11"
                        MinWidth="50"/>
            </Grid>
        </Border>
        
        <!-- Goal List -->
        <Border Grid.Row="1" 
                BorderBrush="{DynamicResource ForegroundBrush}" 
                BorderThickness="1,0,1,1"
                CornerRadius="0,0,6,6"
                Background="{DynamicResource BackgroundBrush}">
            <Grid>
                <!-- Empty State -->
                <StackPanel HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Visibility="{Binding HasNoGoals, Converter={x:Static converters:BoolToVisibilityConverter.Instance}}">
                    <TextBlock Text="No goals yet" 
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource HintTextBrush}"
                               Opacity="0.6"
                               FontSize="13"/>
                    <TextBlock Text="Click '+ Add' to create a goal" 
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource HintTextBrush}"
                               Opacity="0.4"
                               FontSize="11"
                               Margin="0,4,0,0"/>
                </StackPanel>
                
                <!-- Goal List -->
                <ListView ItemsSource="{Binding Goals}"
                          SelectedItem="{Binding SelectedGoal}"
                          BorderThickness="0"
                          Background="Transparent"
                          Visibility="{Binding HasGoals, Converter={x:Static converters:BoolToVisibilityConverter.Instance}}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8" 
                                    BorderBrush="{DynamicResource BorderBrush}" 
                                    BorderThickness="0,0,0,1"
                                    Background="Transparent"
                                    Cursor="Hand">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Title -->
                                    <TextBlock Grid.Column="0" Grid.Row="0"
                                               Text="{Binding Title}"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    
                                    <!-- Status Badge -->
                                    <Border Grid.Column="1" Grid.Row="0"
                                            Background="{Binding Status, Converter={x:Static converters:GoalStatusToColorConverter.Instance}}"
                                            CornerRadius="4"
                                            Padding="6,2">
                                        <TextBlock Text="{Binding Status}"
                                                   FontSize="10"
                                                   Foreground="White"/>
                                    </Border>
                                    
                                    <!-- Progress Bar -->
                                    <Grid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Margin="0,6,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Height="4" 
                                                CornerRadius="2" 
                                                Background="{DynamicResource SurfaceBrush}">
                                            <Border HorizontalAlignment="Left"
                                                    CornerRadius="2"
                                                    Background="{DynamicResource AccentBrush}">
                                                <Border.Width>
                                                    <MultiBinding Converter="{x:Static converters:ProgressWidthConverter.Instance}">
                                                        <Binding Path="Progress"/>
                                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}"/>
                                                    </MultiBinding>
                                                </Border.Width>
                                            </Border>
                                        </Border>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Progress, StringFormat='{}{0:F0}%'}"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource HintTextBrush}"
                                                   Margin="8,0,0,0"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>

