<controls:BaseWindow x:Class="Tracker.Views.Dialogs.InsightsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Tracker.Controls"
        xmlns:local="clr-namespace:Tracker.Views.Dialogs"
        Title="AI Insights" 
        Width="500" Height="600"
        MinWidth="400" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Style="{StaticResource TrackerBaseWindow}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <controls:DialogTitleBar Grid.Row="0"
                                 DialogTitle="AI Insights"
                                 CloseClicked="Close_Click"/>
        
        <!-- Insights Panel -->
        <controls:InsightPanelControl Grid.Row="1" 
                                       x:Name="InsightPanel"
                                       Margin="16"/>
    </Grid>
</controls:BaseWindow>
