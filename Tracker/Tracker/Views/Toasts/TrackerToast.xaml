<Window x:Class="Tracker.Views.Toasts.TrackerToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Tracker.Common.Converters"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Height="90"
        Width="360"
        Opacity="0"
        Loaded="ToastWindow_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Toast type colors -->
            <SolidColorBrush x:Key="InfoAccentBrush" Color="#3B82F6"/>
            <SolidColorBrush x:Key="SuccessAccentBrush" Color="#22C55E"/>
            <SolidColorBrush x:Key="WarningAccentBrush" Color="#F59E0B"/>
            <SolidColorBrush x:Key="ErrorAccentBrush" Color="#EF4444"/>
            
            <!-- Icon paths -->
            <Geometry x:Key="InfoIcon">M13,9H11V7H13M13,17H11V11H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z</Geometry>
            <Geometry x:Key="SuccessIcon">M12 2C6.5 2 2 6.5 2 12S6.5 22 12 22 22 17.5 22 12 17.5 2 12 2M10 17L5 12L6.41 10.59L10 14.17L17.59 6.58L19 8L10 17Z</Geometry>
            <Geometry x:Key="WarningIcon">M13 14H11V10H13M13 18H11V16H13M1 21H23L12 2L1 21Z</Geometry>
            <Geometry x:Key="ErrorIcon">M12,2C17.53,2 22,6.47 22,12C22,17.53 17.53,22 12,22C6.47,22 2,17.53 2,12C2,6.47 6.47,2 12,2M15.59,7L12,10.59L8.41,7L7,8.41L10.59,12L7,15.59L8.41,17L12,13.41L15.59,17L17,15.59L13.41,12L17,8.41L15.59,7Z</Geometry>
            
            <!-- Drop shadow effect -->
            <DropShadowEffect x:Key="ToastShadow" 
                              BlurRadius="20" 
                              ShadowDepth="4" 
                              Direction="270" 
                              Color="#000000" 
                              Opacity="0.4"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border x:Name="ToastBorder"
            CornerRadius="8"
            Background="{DynamicResource PopupBackgroundBrush}"
            BorderThickness="1"
            BorderBrush="{DynamicResource BorderBrush}"
            Effect="{StaticResource ToastShadow}"
            Margin="10">
        <Grid Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Accent bar on left -->
            <Border x:Name="AccentBar"
                    Grid.Column="0"
                    CornerRadius="8,0,0,8"
                    Background="{StaticResource InfoAccentBrush}"/>
            
            <!-- Icon -->
            <Viewbox Grid.Column="1" 
                     Width="28" 
                     Height="28"
                     Margin="8,0,0,0">
                <Path x:Name="ToastIcon"
                      Data="{StaticResource InfoIcon}"
                      Fill="{StaticResource InfoAccentBrush}"/>
            </Viewbox>
            
            <!-- Content -->
            <StackPanel Grid.Column="2" 
                        VerticalAlignment="Center"
                        Margin="8,12,8,12">
                <TextBlock x:Name="ToastTitle"
                           Text="Notification"
                           Foreground="{DynamicResource TextBrush}"
                           FontWeight="SemiBold"
                           FontSize="14"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="ToastMessage"
                           Text="This is a toast notification message."
                           Foreground="{DynamicResource HintTextBrush}"
                           FontSize="12"
                           TextWrapping="Wrap"
                           MaxHeight="36"
                           Margin="0,2,0,0"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            
            <!-- Close button -->
            <Button x:Name="CloseButton"
                    Grid.Column="3"
                    Width="28"
                    Height="28"
                    Margin="0,8,8,0"
                    VerticalAlignment="Top"
                    Cursor="Hand"
                    Click="Toast_Close">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4">
                                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                                              Fill="{DynamicResource HintTextBrush}"
                                              Width="12"
                                              Height="12"
                                              Stretch="Uniform"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            
            <!-- Progress bar for auto-dismiss -->
            <Border Grid.Column="0"
                    Grid.ColumnSpan="4"
                    VerticalAlignment="Bottom"
                    Height="3"
                    CornerRadius="0,0,8,8"
                    ClipToBounds="True">
                <Border x:Name="ProgressBar"
                        Background="{StaticResource InfoAccentBrush}"
                        HorizontalAlignment="Left"
                        Opacity="0.6"
                        CornerRadius="0,0,0,8">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleX="1"/>
                    </Border.RenderTransform>
                </Border>
            </Border>
        </Grid>
    </Border>
</Window>
