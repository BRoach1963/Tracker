<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="using:ProCohere.Avalonia.Models"
        xmlns:asyncImageLoader="using:AsyncImageLoader"
        xmlns:markup="clr-namespace:ProCohere.Avalonia.Markup"
        mc:Ignorable="d" d:DesignWidth="520" d:DesignHeight="680"
        x:Class="ProCohere.Avalonia.Views.Dialogs.EditTeamMemberDialog"
        Title="Team Member"
        Width="520" Height="680"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False"
        SystemDecorations="BorderOnly"
        ExtendClientAreaToDecorationsHint="True"
        Background="{DynamicResource SystemChromeMediumLowColor}">

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                Padding="20,16">
            <TextBlock x:Name="DialogTitle"
                       Text="{markup:Localize Key=Dialog_TeamMember_AddTitle}"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        </Border>
        
        <!-- Content -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="20,16">
            <StackPanel Spacing="16">
                
                <!-- Avatar Section -->
                <StackPanel HorizontalAlignment="Center" Spacing="8">
                    <Grid Width="80" Height="80">
                        <!-- Avatar Image -->
                        <Border x:Name="AvatarBorder"
                                Width="80" Height="80"
                                CornerRadius="40"
                                ClipToBounds="True"
                                IsVisible="False">
                            <Image x:Name="AvatarImage"
                                   Stretch="UniformToFill" />
                        </Border>
                        <!-- Initials fallback -->
                        <Border x:Name="InitialsBorder"
                                Width="80" Height="80"
                                CornerRadius="40"
                                Background="{DynamicResource AccentFillColorDefaultBrush}">
                            <TextBlock x:Name="InitialsText"
                                       Text="?"
                                       FontSize="28"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                        <Button x:Name="UploadAvatarButton"
                                Content="{markup:Localize Key=Dialog_TeamMember_UploadPhoto}"
                                Click="UploadAvatarButton_Click" />
                        <Button x:Name="RemoveAvatarButton"
                                Content="{markup:Localize Key=Dialog_TeamMember_Remove}"
                                IsVisible="False"
                                Click="RemoveAvatarButton_Click" />
                    </StackPanel>
                </StackPanel>
                
                <!-- Name Row -->
                <Grid ColumnDefinitions="*,12,*">
                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_FirstName}" FontSize="12" />
                        <TextBox x:Name="FirstNameTextBox"
                                 Watermark="{markup:Localize Key=Dialog_TeamMember_FirstNameWatermark}"
                                 TextChanged="NameTextBox_TextChanged" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_LastName}" FontSize="12" />
                        <TextBox x:Name="LastNameTextBox"
                                 Watermark="{markup:Localize Key=Dialog_TeamMember_LastNameWatermark}"
                                 TextChanged="NameTextBox_TextChanged" />
                    </StackPanel>
                </Grid>
                
                <!-- Email -->
                <StackPanel Spacing="4">
                    <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_Email}" FontSize="12" />
                    <TextBox x:Name="EmailTextBox"
                             Watermark="{markup:Localize Key=Dialog_TeamMember_EmailWatermark}" />
                </StackPanel>
                
                <!-- Job Title -->
                <StackPanel Spacing="4">
                    <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_JobTitle}" FontSize="12" />
                    <TextBox x:Name="JobTitleTextBox"
                             Watermark="{markup:Localize Key=Dialog_TeamMember_JobTitleWatermark}" />
                </StackPanel>
                
                <!-- Phone -->
                <StackPanel Spacing="4">
                    <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_Phone}" FontSize="12" />
                    <TextBox x:Name="PhoneTextBox"
                             Watermark="{markup:Localize Key=Dialog_TeamMember_PhoneWatermark}" />
                </StackPanel>
                
                <!-- Manager -->
                <StackPanel Spacing="4">
                    <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_Manager}" FontSize="12" />
                    <ComboBox x:Name="ManagerComboBox"
                              HorizontalAlignment="Stretch"
                              PlaceholderText="{markup:Localize Key=Dialog_TeamMember_SelectManager}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="models:TeamMemberDetail">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Border Width="24" Height="24" CornerRadius="12" ClipToBounds="True">
                                        <Panel>
                                            <Border Background="{DynamicResource AccentFillColorDefaultBrush}"
                                                    CornerRadius="12"
                                                    IsVisible="{Binding UserAvatarUrl, Converter={x:Static StringConverters.IsNullOrEmpty}}">
                                                <TextBlock Text="{Binding Initials}"
                                                           FontSize="10"
                                                           Foreground="White"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" />
                                            </Border>
                                            <Image asyncImageLoader:ImageLoader.Source="{Binding UserAvatarUrl}"
                                                   Stretch="UniformToFill" Width="24" Height="24"
                                                   IsVisible="{Binding UserAvatarUrl, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                        </Panel>
                                    </Border>
                                    <TextBlock Text="{Binding FullName}"
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                
                <!-- Dates Row -->
                <Grid ColumnDefinitions="*,12,*">
                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_Birthday}" FontSize="12" />
                        <CalendarDatePicker x:Name="BirthdayPicker"
                                            HorizontalAlignment="Stretch"
                                            Watermark="{markup:Localize Key=Dialog_TeamMember_SelectDate}"
                                            IsTodayHighlighted="True"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Spacing="4">
                        <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_HireDate}" FontSize="12" />
                        <CalendarDatePicker x:Name="HireDatePicker"
                                            HorizontalAlignment="Stretch"
                                            Watermark="{markup:Localize Key=Dialog_TeamMember_SelectDate}"
                                            IsTodayHighlighted="True"/>
                    </StackPanel>
                </Grid>
                
                <!-- Social Links -->
                <StackPanel Spacing="4">
                    <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_LinkedIn}" FontSize="12" />
                    <TextBox x:Name="LinkedInTextBox"
                             Watermark="{markup:Localize Key=Dialog_TeamMember_LinkedInWatermark}" />
                </StackPanel>
                
                <!-- Notes Section -->
                <StackPanel x:Name="NotesSection" Spacing="8" IsVisible="False">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Text="{markup:Localize Key=Dialog_TeamMember_Notes}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <Button x:Name="AddNoteButton" Grid.Column="1" Content="{markup:Localize Key=Dialog_TeamMember_AddNote}" 
                                Classes="accent" Padding="8,4" FontSize="11"
                                Click="AddNoteButton_Click" />
                    </Grid>
                    
                    <!-- Notes List -->
                    <ItemsControl x:Name="NotesItemsControl" MaxHeight="200">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="models:Note">
                                <Border Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="6" Padding="10" Margin="0,0,0,6">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <StackPanel Grid.Column="0" Spacing="4">
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="12"
                                                       IsVisible="{Binding Title, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                                            <TextBlock Text="{Binding Content}" FontSize="11" TextWrapping="Wrap"
                                                       Foreground="{DynamicResource BrushTextSecondary}"
                                                       MaxLines="3" />
                                            <TextBlock FontSize="10" Foreground="{DynamicResource BrushTextTertiary}">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0:MMM d, yyyy}">
                                                        <Binding Path="CreatedAt" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="✕" Padding="4,2" FontSize="10"
                                                VerticalAlignment="Top" Classes="subtle"
                                                Tag="{Binding Id}"
                                                Click="DeleteNoteButton_Click" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <TextBlock x:Name="NoNotesText" Text="{markup:Localize Key=Dialog_TeamMember_NoNotes}" FontSize="11" FontStyle="Italic"
                               Foreground="{DynamicResource BrushTextTertiary}" IsVisible="False" />
                </StackPanel>
                
                <!-- Active Status (only in edit mode) -->
                <StackPanel x:Name="StatusSection" IsVisible="False">
                    <CheckBox x:Name="IsActiveCheckBox"
                              Content="{markup:Localize Key=Dialog_TeamMember_ActiveMember}"
                              IsChecked="True" />
                </StackPanel>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Footer -->
        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                Padding="20,12">
            <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                <Button x:Name="DeleteButton"
                        Grid.Column="0"
                        Content="{markup:Localize Key=Button_Delete}"
                        Foreground="#E74C3C"
                        IsVisible="False"
                        Click="DeleteButton_Click" />
                <Button Grid.Column="2"
                        Content="{markup:Localize Key=Button_Cancel}"
                        Classes="cancel-btn"
                        MinWidth="80"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click" />
                <Button Grid.Column="3"
                        x:Name="SaveButton"
                        Content="{markup:Localize Key=Dialog_TeamMember_AddMember}"
                        Classes="accent"
                        MinWidth="100"
                        Click="SaveButton_Click" />
            </Grid>
        </Border>
    </Grid>
</Window>
