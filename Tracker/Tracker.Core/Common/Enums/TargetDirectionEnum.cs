namespace Tracker.Core.Common.Enums
{
    public enum TargetDirectionEnum
    {
        GreaterOrEqual, // Success is when value >= target
        LessOrEqual     // Success is when value <= target
    }
}
