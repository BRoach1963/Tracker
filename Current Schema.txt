-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.action_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  meeting_id uuid NOT NULL,
  assignee_team_member_id uuid,
  title character varying NOT NULL,
  description text,
  due_date date,
  is_completed boolean NOT NULL DEFAULT false,
  completed_at timestamp with time zone,
  converted_task_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT action_items_pkey PRIMARY KEY (id),
  CONSTRAINT action_items_meeting_id_fkey FOREIGN KEY (meeting_id) REFERENCES public.meetings(id),
  CONSTRAINT action_items_assignee_team_member_id_fkey FOREIGN KEY (assignee_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT action_items_converted_task_id_fkey FOREIGN KEY (converted_task_id) REFERENCES public.tasks(id)
);
CREATE TABLE public.activity_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  actor_user_id uuid NOT NULL,
  actor_team_member_id uuid,
  action character varying NOT NULL,
  entity_type character varying NOT NULL,
  entity_id uuid NOT NULL,
  entity_name character varying,
  old_values jsonb,
  new_values jsonb,
  context_type character varying,
  ip_address inet,
  user_agent text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT activity_log_pkey PRIMARY KEY (id),
  CONSTRAINT activity_log_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT activity_log_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES public.users(id),
  CONSTRAINT activity_log_actor_team_member_id_fkey FOREIGN KEY (actor_team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.ai_conversations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  title character varying,
  context_entity_type character varying,
  context_entity_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT ai_conversations_pkey PRIMARY KEY (id),
  CONSTRAINT ai_conversations_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT ai_conversations_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.ai_insights (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  target_team_id uuid,
  target_team_member_id uuid,
  insight_type character varying NOT NULL,
  category character varying NOT NULL,
  title character varying NOT NULL,
  summary text NOT NULL,
  details jsonb,
  priority character varying NOT NULL DEFAULT 'medium'::character varying,
  recommended_actions jsonb,
  source_entities jsonb,
  valid_from timestamp with time zone NOT NULL DEFAULT now(),
  valid_until timestamp with time zone,
  is_dismissed boolean NOT NULL DEFAULT false,
  dismissed_at timestamp with time zone,
  dismissed_by uuid,
  dismiss_reason text,
  is_actioned boolean NOT NULL DEFAULT false,
  actioned_at timestamp with time zone,
  action_notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  unique_key character varying,
  is_read boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  severity character varying NOT NULL DEFAULT 'info'::character varying,
  description text,
  action_suggestion text,
  entity_type character varying,
  entity_id uuid,
  generated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_insights_pkey PRIMARY KEY (id),
  CONSTRAINT ai_insights_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT ai_insights_target_team_id_fkey FOREIGN KEY (target_team_id) REFERENCES public.teams(id),
  CONSTRAINT ai_insights_target_team_member_id_fkey FOREIGN KEY (target_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT ai_insights_dismissed_by_fkey FOREIGN KEY (dismissed_by) REFERENCES public.users(id)
);
CREATE TABLE public.ai_messages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  conversation_id uuid NOT NULL,
  role character varying NOT NULL,
  content text NOT NULL,
  prompt_tokens integer,
  completion_tokens integer,
  total_tokens integer,
  model_name character varying,
  referenced_entities jsonb,
  message_order integer NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_messages_pkey PRIMARY KEY (id),
  CONSTRAINT ai_messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.ai_conversations(id)
);
CREATE TABLE public.announcement_reads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  announcement_id uuid NOT NULL,
  user_id uuid NOT NULL,
  read_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT announcement_reads_pkey PRIMARY KEY (id),
  CONSTRAINT announcement_reads_announcement_id_fkey FOREIGN KEY (announcement_id) REFERENCES public.announcements(id),
  CONSTRAINT announcement_reads_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.announcements (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  created_by_user_id uuid NOT NULL,
  title character varying NOT NULL,
  content text NOT NULL,
  target_type character varying NOT NULL DEFAULT 'organization'::character varying,
  target_team_id uuid,
  target_role_ids jsonb,
  publish_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone,
  is_pinned boolean NOT NULL DEFAULT false,
  priority character varying NOT NULL DEFAULT 'normal'::character varying,
  is_published boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT announcements_pkey PRIMARY KEY (id),
  CONSTRAINT announcements_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT announcements_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT announcements_target_team_id_fkey FOREIGN KEY (target_team_id) REFERENCES public.teams(id)
);
CREATE TABLE public.calendar_links (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  provider USER-DEFINED NOT NULL,
  account_email character varying,
  account_name character varying,
  access_token text,
  refresh_token text,
  token_expires_at timestamp with time zone,
  is_active boolean NOT NULL DEFAULT true,
  sync_enabled boolean NOT NULL DEFAULT true,
  sync_meetings_to_calendar boolean NOT NULL DEFAULT true,
  sync_tasks_to_calendar boolean NOT NULL DEFAULT false,
  create_meeting_from_calendar boolean NOT NULL DEFAULT false,
  default_calendar_id character varying,
  default_calendar_name character varying,
  last_sync_at timestamp with time zone,
  last_sync_status USER-DEFINED,
  last_sync_error text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  sync_token text,
  CONSTRAINT calendar_links_pkey PRIMARY KEY (id),
  CONSTRAINT calendar_links_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.development_goal_comments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  goal_id uuid NOT NULL,
  author_team_member_id uuid NOT NULL,
  content text NOT NULL,
  comment_type character varying NOT NULL DEFAULT 'comment'::character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT development_goal_comments_pkey PRIMARY KEY (id),
  CONSTRAINT development_goal_comments_goal_id_fkey FOREIGN KEY (goal_id) REFERENCES public.development_goals(id),
  CONSTRAINT development_goal_comments_author_team_member_id_fkey FOREIGN KEY (author_team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.development_goal_milestones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  goal_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  target_date date,
  completed_at timestamp with time zone,
  status USER-DEFINED NOT NULL DEFAULT 'not_started'::milestone_status,
  sort_order integer NOT NULL DEFAULT 0,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT development_goal_milestones_pkey PRIMARY KEY (id),
  CONSTRAINT development_goal_milestones_goal_id_fkey FOREIGN KEY (goal_id) REFERENCES public.development_goals(id)
);
CREATE TABLE public.development_goals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  category USER-DEFINED NOT NULL DEFAULT 'skill_development'::dev_goal_category,
  target_date date,
  started_at timestamp with time zone,
  completed_at timestamp with time zone,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::dev_goal_status,
  progress_percent integer DEFAULT 0 CHECK (progress_percent >= 0 AND progress_percent <= 100),
  why_important text,
  success_criteria text,
  support_needed text,
  resources text,
  is_private boolean NOT NULL DEFAULT false,
  shared_with_manager boolean NOT NULL DEFAULT true,
  review_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT development_goals_pkey PRIMARY KEY (id),
  CONSTRAINT development_goals_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT development_goals_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.feedback (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  from_team_member_id uuid NOT NULL,
  to_team_member_id uuid NOT NULL,
  feedback_type USER-DEFINED NOT NULL DEFAULT 'general'::feedback_type,
  sentiment USER-DEFINED NOT NULL DEFAULT 'neutral'::feedback_sentiment,
  content text NOT NULL,
  context_type character varying,
  context_id uuid,
  is_private boolean NOT NULL DEFAULT false,
  is_requested boolean NOT NULL DEFAULT false,
  request_id uuid,
  ai_summary text,
  ai_tags jsonb,
  is_acknowledged boolean NOT NULL DEFAULT false,
  acknowledged_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  CONSTRAINT feedback_pkey PRIMARY KEY (id),
  CONSTRAINT feedback_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT feedback_from_team_member_id_fkey FOREIGN KEY (from_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT feedback_to_team_member_id_fkey FOREIGN KEY (to_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT feedback_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.feedback_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  requester_team_member_id uuid NOT NULL,
  requested_from_team_member_id uuid NOT NULL,
  about_team_member_id uuid NOT NULL,
  message text,
  context_type character varying,
  context_id uuid,
  due_date date,
  status character varying NOT NULL DEFAULT 'pending'::character varying,
  completed_at timestamp with time zone,
  declined_at timestamp with time zone,
  decline_reason text,
  response_feedback_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_requests_pkey PRIMARY KEY (id),
  CONSTRAINT feedback_requests_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT feedback_requests_requester_team_member_id_fkey FOREIGN KEY (requester_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT feedback_requests_requested_from_team_member_id_fkey FOREIGN KEY (requested_from_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT feedback_requests_about_team_member_id_fkey FOREIGN KEY (about_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT feedback_requests_response_feedback_id_fkey FOREIGN KEY (response_feedback_id) REFERENCES public.feedback(id)
);
CREATE TABLE public.goal_milestones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  goal_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  target_date date NOT NULL,
  completed_date date,
  is_completed boolean NOT NULL DEFAULT false,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT goal_milestones_pkey PRIMARY KEY (id),
  CONSTRAINT goal_milestones_goal_id_fkey FOREIGN KEY (goal_id) REFERENCES public.goals(id)
);
CREATE TABLE public.goals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  owner_team_member_id uuid,
  created_by_user_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  time_period USER-DEFINED NOT NULL DEFAULT 'q1'::goal_time_period,
  year integer NOT NULL DEFAULT EXTRACT(year FROM CURRENT_DATE),
  start_date date NOT NULL,
  end_date date NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'not_started'::goal_status,
  status_override USER-DEFINED,
  progress_percent numeric NOT NULL DEFAULT 0 CHECK (progress_percent >= 0::numeric AND progress_percent <= 100::numeric),
  progress_override numeric,
  is_team_visible boolean NOT NULL DEFAULT true,
  is_org_visible boolean NOT NULL DEFAULT false,
  project_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  type USER-DEFINED NOT NULL DEFAULT 'organizational'::goal_type,
  source_agenda_item_id uuid,
  source_meeting_id uuid,
  CONSTRAINT goals_pkey PRIMARY KEY (id),
  CONSTRAINT goals_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT goals_owner_team_member_id_fkey FOREIGN KEY (owner_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT goals_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT goals_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id),
  CONSTRAINT fk_goals_project FOREIGN KEY (project_id) REFERENCES public.projects(id)
);
CREATE TABLE public.journal_entries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  team_member_id uuid NOT NULL,
  entry_date date NOT NULL,
  content text NOT NULL,
  mood_rating integer,
  energy_level integer,
  wins jsonb,
  challenges jsonb,
  grateful_for jsonb,
  progress_on_goals text,
  ai_insights text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  CONSTRAINT journal_entries_pkey PRIMARY KEY (id),
  CONSTRAINT journal_entries_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.manager_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  manager_user_id uuid NOT NULL,
  start_date date NOT NULL DEFAULT CURRENT_DATE,
  end_date date,
  change_reason character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  CONSTRAINT manager_history_pkey PRIMARY KEY (id),
  CONSTRAINT manager_history_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT manager_history_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT manager_history_manager_user_id_fkey FOREIGN KEY (manager_user_id) REFERENCES public.users(id),
  CONSTRAINT manager_history_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.meeting_agenda_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  meeting_id uuid NOT NULL,
  added_by_team_member_id uuid,
  title character varying NOT NULL,
  notes text,
  sort_order integer NOT NULL DEFAULT 0,
  is_discussed boolean NOT NULL DEFAULT false,
  discussed_at timestamp with time zone,
  time_estimate_minutes integer,
  actual_duration_minutes integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  linked_entity_type character varying,
  linked_entity_id uuid,
  CONSTRAINT meeting_agenda_items_pkey PRIMARY KEY (id),
  CONSTRAINT meeting_agenda_items_meeting_id_fkey FOREIGN KEY (meeting_id) REFERENCES public.meetings(id),
  CONSTRAINT meeting_agenda_items_added_by_team_member_id_fkey FOREIGN KEY (added_by_team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.meeting_attendees (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  meeting_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  response character varying DEFAULT 'pending'::character varying,
  response_at timestamp with time zone,
  attended boolean,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  external_attendee_email character varying,
  removed_from_calendar_at timestamp with time zone,
  sync_status character varying DEFAULT 'synced'::character varying,
  CONSTRAINT meeting_attendees_pkey PRIMARY KEY (id),
  CONSTRAINT meeting_attendees_meeting_id_fkey FOREIGN KEY (meeting_id) REFERENCES public.meetings(id),
  CONSTRAINT meeting_attendees_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.meeting_notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  meeting_id uuid NOT NULL,
  author_team_member_id uuid,
  content text NOT NULL,
  is_private boolean NOT NULL DEFAULT false,
  ai_summary text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  CONSTRAINT meeting_notes_pkey PRIMARY KEY (id),
  CONSTRAINT meeting_notes_meeting_id_fkey FOREIGN KEY (meeting_id) REFERENCES public.meetings(id),
  CONSTRAINT meeting_notes_author_team_member_id_fkey FOREIGN KEY (author_team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.meeting_template_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  title character varying NOT NULL,
  notes text,
  time_estimate_minutes integer,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT meeting_template_items_pkey PRIMARY KEY (id),
  CONSTRAINT meeting_template_items_template_id_fkey FOREIGN KEY (template_id) REFERENCES public.meeting_templates(id)
);
CREATE TABLE public.meeting_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  created_by_user_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  meeting_type character varying NOT NULL DEFAULT 'one_on_one'::character varying,
  suggested_duration_minutes integer NOT NULL DEFAULT 30,
  sort_order integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  CONSTRAINT meeting_templates_pkey PRIMARY KEY (id),
  CONSTRAINT meeting_templates_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT meeting_templates_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT meeting_templates_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.meetings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  created_by_user_id uuid NOT NULL,
  meeting_type USER-DEFINED NOT NULL DEFAULT 'one_on_one'::meeting_type,
  manager_team_member_id uuid,
  report_team_member_id uuid,
  team_id uuid,
  title character varying NOT NULL,
  description text,
  scheduled_at timestamp with time zone,
  duration_minutes integer NOT NULL DEFAULT 30,
  recurrence_rule character varying,
  location character varying,
  status USER-DEFINED NOT NULL DEFAULT 'scheduled'::meeting_status,
  started_at timestamp with time zone,
  ended_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  calendar_event_id character varying,
  calendar_provider USER-DEFINED,
  calendar_link_id uuid,
  video_conference_url text,
  video_conference_provider character varying,
  calendar_sync_status USER-DEFINED,
  last_synced_at timestamp with time zone,
  project_id uuid,
  calendar_etag character varying,
  video_conference_id character varying,
  CONSTRAINT meetings_pkey PRIMARY KEY (id),
  CONSTRAINT meetings_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT meetings_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT meetings_manager_team_member_id_fkey FOREIGN KEY (manager_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT meetings_report_team_member_id_fkey FOREIGN KEY (report_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT meetings_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.teams(id),
  CONSTRAINT meetings_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id),
  CONSTRAINT meetings_calendar_link_id_fkey FOREIGN KEY (calendar_link_id) REFERENCES public.calendar_links(id),
  CONSTRAINT meetings_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.projects(id)
);
CREATE TABLE public.metric_data_sources (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  metric_id uuid NOT NULL,
  source_type character varying NOT NULL,
  source_id uuid,
  source_config jsonb,
  aggregation_type character varying NOT NULL DEFAULT 'latest'::character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT metric_data_sources_pkey PRIMARY KEY (id),
  CONSTRAINT metric_data_sources_metric_id_fkey FOREIGN KEY (metric_id) REFERENCES public.metrics(id)
);
CREATE TABLE public.metric_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  metric_id uuid NOT NULL,
  value numeric NOT NULL,
  recorded_at timestamp with time zone NOT NULL DEFAULT now(),
  recorded_by_user_id uuid,
  source character varying DEFAULT 'manual'::character varying,
  notes text,
  CONSTRAINT metric_history_pkey PRIMARY KEY (id),
  CONSTRAINT metric_history_metric_id_fkey FOREIGN KEY (metric_id) REFERENCES public.metrics(id),
  CONSTRAINT metric_history_recorded_by_user_id_fkey FOREIGN KEY (recorded_by_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.metrics (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  owner_team_member_id uuid,
  created_by_user_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  category character varying,
  current_value numeric NOT NULL DEFAULT 0,
  target_value numeric,
  baseline_value numeric,
  unit character varying,
  target_direction USER-DEFINED NOT NULL DEFAULT 'higher_is_better'::metric_target_direction,
  frequency USER-DEFINED NOT NULL DEFAULT 'monthly'::metric_frequency,
  last_updated_at timestamp with time zone DEFAULT now(),
  is_composite boolean NOT NULL DEFAULT false,
  parent_metric_id uuid,
  is_team_visible boolean NOT NULL DEFAULT true,
  is_org_visible boolean NOT NULL DEFAULT false,
  warning_threshold numeric,
  critical_threshold numeric,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  CONSTRAINT metrics_pkey PRIMARY KEY (id),
  CONSTRAINT metrics_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT metrics_owner_team_member_id_fkey FOREIGN KEY (owner_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT metrics_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT metrics_parent_metric_id_fkey FOREIGN KEY (parent_metric_id) REFERENCES public.metrics(id),
  CONSTRAINT metrics_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.milestones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  project_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  target_date date NOT NULL,
  completed_date date,
  is_completed boolean NOT NULL DEFAULT false,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT milestones_pkey PRIMARY KEY (id),
  CONSTRAINT milestones_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.projects(id)
);
CREATE TABLE public.note_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  created_by_user_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  content_template text NOT NULL,
  template_type character varying NOT NULL,
  is_personal boolean NOT NULL DEFAULT true,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT note_templates_pkey PRIMARY KEY (id),
  CONSTRAINT note_templates_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT note_templates_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  author_team_member_id uuid NOT NULL,
  title character varying,
  content text NOT NULL,
  content_format character varying NOT NULL DEFAULT 'plain'::character varying,
  linked_team_member_id uuid,
  linked_meeting_id uuid,
  linked_project_id uuid,
  linked_goal_id uuid,
  linked_task_id uuid,
  category character varying,
  tags jsonb,
  is_private boolean NOT NULL DEFAULT true,
  is_pinned boolean NOT NULL DEFAULT false,
  pinned_at timestamp with time zone,
  ai_summary text,
  ai_suggested_actions jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  is_archived boolean NOT NULL DEFAULT false,
  archived_at timestamp with time zone,
  CONSTRAINT notes_pkey PRIMARY KEY (id),
  CONSTRAINT notes_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT notes_author_team_member_id_fkey FOREIGN KEY (author_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT notes_linked_team_member_id_fkey FOREIGN KEY (linked_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT notes_linked_meeting_id_fkey FOREIGN KEY (linked_meeting_id) REFERENCES public.meetings(id),
  CONSTRAINT notes_linked_project_id_fkey FOREIGN KEY (linked_project_id) REFERENCES public.projects(id),
  CONSTRAINT notes_linked_goal_id_fkey FOREIGN KEY (linked_goal_id) REFERENCES public.goals(id),
  CONSTRAINT notes_linked_task_id_fkey FOREIGN KEY (linked_task_id) REFERENCES public.tasks(id),
  CONSTRAINT notes_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.notification_preferences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  notification_type character varying NOT NULL,
  in_app_enabled boolean NOT NULL DEFAULT true,
  email_enabled boolean NOT NULL DEFAULT true,
  push_enabled boolean NOT NULL DEFAULT false,
  email_frequency character varying DEFAULT 'immediate'::character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT notification_preferences_pkey PRIMARY KEY (id),
  CONSTRAINT notification_preferences_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  user_id uuid NOT NULL,
  notification_type character varying NOT NULL,
  title character varying NOT NULL,
  message text NOT NULL,
  entity_type character varying,
  entity_id uuid,
  action_url character varying,
  priority character varying NOT NULL DEFAULT 'normal'::character varying,
  is_read boolean NOT NULL DEFAULT false,
  read_at timestamp with time zone,
  is_dismissed boolean NOT NULL DEFAULT false,
  dismissed_at timestamp with time zone,
  email_sent boolean NOT NULL DEFAULT false,
  email_sent_at timestamp with time zone,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.organization_snapshots (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  snapshot_date date NOT NULL,
  period_type USER-DEFINED NOT NULL DEFAULT 'weekly'::snapshot_period,
  period_start date NOT NULL,
  period_end date NOT NULL,
  total_users integer DEFAULT 0,
  active_users integer DEFAULT 0,
  total_team_members integer DEFAULT 0,
  users_logged_in integer DEFAULT 0,
  login_rate numeric,
  goals_total integer DEFAULT 0,
  goals_on_track_rate numeric,
  goals_completed_this_period integer DEFAULT 0,
  one_on_ones_held integer DEFAULT 0,
  one_on_one_completion_rate numeric,
  avg_engagement_score numeric,
  enps_score integer,
  feedback_count integer DEFAULT 0,
  recognition_count integer DEFAULT 0,
  reviews_in_progress integer DEFAULT 0,
  reviews_completed integer DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_snapshots_pkey PRIMARY KEY (id),
  CONSTRAINT organization_snapshots_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.organizations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL,
  slug character varying UNIQUE,
  subscription_tier character varying NOT NULL DEFAULT 'free'::character varying CHECK (subscription_tier::text = ANY (ARRAY['free'::character varying, 'professional'::character varying, 'enterprise'::character varying]::text[])),
  max_users integer DEFAULT 5,
  max_team_members integer DEFAULT 25,
  settings jsonb DEFAULT '{}'::jsonb,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by character varying,
  CONSTRAINT organizations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.progress_snapshots (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  entity_type character varying NOT NULL,
  entity_id uuid NOT NULL,
  snapshot_date date NOT NULL,
  period_type USER-DEFINED NOT NULL DEFAULT 'weekly'::snapshot_period,
  metrics jsonb NOT NULL DEFAULT '{}'::jsonb,
  overall_score numeric,
  trend_direction integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT progress_snapshots_pkey PRIMARY KEY (id),
  CONSTRAINT progress_snapshots_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.project_dependencies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  dependent_project_id uuid NOT NULL,
  required_project_id uuid NOT NULL,
  dependency_type character varying NOT NULL DEFAULT 'finish_to_start'::character varying,
  description text,
  is_blocking boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by_user_id uuid,
  CONSTRAINT project_dependencies_pkey PRIMARY KEY (id),
  CONSTRAINT project_dependencies_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT project_dependencies_dependent_project_id_fkey FOREIGN KEY (dependent_project_id) REFERENCES public.projects(id),
  CONSTRAINT project_dependencies_required_project_id_fkey FOREIGN KEY (required_project_id) REFERENCES public.projects(id),
  CONSTRAINT project_dependencies_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.project_members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  project_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  role character varying,
  joined_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT project_members_pkey PRIMARY KEY (id),
  CONSTRAINT project_members_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.projects(id),
  CONSTRAINT project_members_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.projects (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  owner_team_member_id uuid,
  created_by_user_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  color character varying,
  start_date date,
  target_end_date date,
  actual_end_date date,
  status USER-DEFINED NOT NULL DEFAULT 'not_started'::task_status,
  progress_percent numeric NOT NULL DEFAULT 0,
  priority USER-DEFINED NOT NULL DEFAULT 'medium'::task_priority,
  is_team_visible boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  source_agenda_item_id uuid,
  source_meeting_id uuid,
  CONSTRAINT projects_pkey PRIMARY KEY (id),
  CONSTRAINT projects_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT projects_owner_team_member_id_fkey FOREIGN KEY (owner_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT projects_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT projects_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.recognition (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  from_team_member_id uuid NOT NULL,
  to_team_member_id uuid NOT NULL,
  title character varying NOT NULL,
  message text NOT NULL,
  badge_type character varying,
  project_id uuid,
  goal_id uuid,
  company_values jsonb,
  is_public boolean NOT NULL DEFAULT true,
  reactions_count integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  CONSTRAINT recognition_pkey PRIMARY KEY (id),
  CONSTRAINT recognition_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT recognition_from_team_member_id_fkey FOREIGN KEY (from_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT recognition_to_team_member_id_fkey FOREIGN KEY (to_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT recognition_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.projects(id),
  CONSTRAINT recognition_goal_id_fkey FOREIGN KEY (goal_id) REFERENCES public.goals(id),
  CONSTRAINT recognition_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.recognition_reactions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  recognition_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  reaction_type character varying NOT NULL DEFAULT 'like'::character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT recognition_reactions_pkey PRIMARY KEY (id),
  CONSTRAINT recognition_reactions_recognition_id_fkey FOREIGN KEY (recognition_id) REFERENCES public.recognition(id),
  CONSTRAINT recognition_reactions_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.reminder_preferences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  entity_type character varying NOT NULL,
  sub_type character varying,
  enabled boolean NOT NULL DEFAULT true,
  default_minutes_before integer NOT NULL DEFAULT 15,
  send_push boolean NOT NULL DEFAULT true,
  send_email boolean NOT NULL DEFAULT false,
  send_in_app boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT reminder_preferences_pkey PRIMARY KEY (id),
  CONSTRAINT reminder_preferences_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.reminders (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  user_id uuid NOT NULL,
  team_member_id uuid,
  reminder_type USER-DEFINED NOT NULL,
  entity_type character varying NOT NULL,
  entity_id uuid NOT NULL,
  title character varying NOT NULL,
  message text,
  remind_at timestamp with time zone NOT NULL,
  minutes_before integer,
  status USER-DEFINED NOT NULL DEFAULT 'scheduled'::reminder_status,
  sent_at timestamp with time zone,
  dismissed_at timestamp with time zone,
  snoozed_until timestamp with time zone,
  send_push boolean NOT NULL DEFAULT true,
  send_email boolean NOT NULL DEFAULT false,
  send_in_app boolean NOT NULL DEFAULT true,
  is_recurring boolean NOT NULL DEFAULT false,
  recurrence_rule character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT reminders_pkey PRIMARY KEY (id),
  CONSTRAINT reminders_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT reminders_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT reminders_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.risks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  entity_type character varying NOT NULL,
  entity_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  severity USER-DEFINED NOT NULL DEFAULT 'medium'::risk_severity,
  probability character varying DEFAULT 'possible'::character varying,
  impact character varying DEFAULT 'moderate'::character varying,
  status USER-DEFINED NOT NULL DEFAULT 'identified'::risk_status,
  mitigation_strategy text,
  contingency_plan text,
  owner_team_member_id uuid,
  identified_date date NOT NULL DEFAULT CURRENT_DATE,
  target_resolution_date date,
  resolved_date date,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by_user_id uuid,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  CONSTRAINT risks_pkey PRIMARY KEY (id),
  CONSTRAINT risks_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT risks_owner_team_member_id_fkey FOREIGN KEY (owner_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT risks_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT risks_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL UNIQUE,
  display_name character varying NOT NULL,
  description text,
  can_manage_org boolean NOT NULL DEFAULT false,
  can_manage_billing boolean NOT NULL DEFAULT false,
  can_manage_users boolean NOT NULL DEFAULT false,
  can_invite_users boolean NOT NULL DEFAULT false,
  can_assign_roles boolean NOT NULL DEFAULT false,
  can_manage_teams boolean NOT NULL DEFAULT false,
  can_create_teams boolean NOT NULL DEFAULT false,
  can_create_goals boolean NOT NULL DEFAULT false,
  can_edit_all_goals boolean NOT NULL DEFAULT false,
  can_edit_own_goals boolean NOT NULL DEFAULT false,
  can_view_team_goals boolean NOT NULL DEFAULT false,
  can_view_org_goals boolean NOT NULL DEFAULT false,
  can_create_metrics boolean NOT NULL DEFAULT false,
  can_edit_metrics boolean NOT NULL DEFAULT false,
  can_view_team_metrics boolean NOT NULL DEFAULT false,
  can_view_org_metrics boolean NOT NULL DEFAULT false,
  can_create_tasks boolean NOT NULL DEFAULT false,
  can_assign_tasks boolean NOT NULL DEFAULT false,
  can_view_team_tasks boolean NOT NULL DEFAULT false,
  can_schedule_meetings boolean NOT NULL DEFAULT false,
  can_run_meetings boolean NOT NULL DEFAULT false,
  can_participate_meetings boolean NOT NULL DEFAULT false,
  can_view_meeting_notes boolean NOT NULL DEFAULT false,
  can_give_feedback boolean NOT NULL DEFAULT false,
  can_receive_feedback boolean NOT NULL DEFAULT false,
  can_view_team_feedback boolean NOT NULL DEFAULT false,
  can_view_team_analytics boolean NOT NULL DEFAULT false,
  can_view_org_analytics boolean NOT NULL DEFAULT false,
  can_export_data boolean NOT NULL DEFAULT false,
  is_system_role boolean NOT NULL DEFAULT false,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT roles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.survey_answers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  response_id uuid NOT NULL,
  question_id uuid NOT NULL,
  rating_value integer,
  text_value text,
  selected_options jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT survey_answers_pkey PRIMARY KEY (id),
  CONSTRAINT survey_answers_response_id_fkey FOREIGN KEY (response_id) REFERENCES public.survey_responses(id),
  CONSTRAINT survey_answers_question_id_fkey FOREIGN KEY (question_id) REFERENCES public.survey_questions(id)
);
CREATE TABLE public.survey_instances (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  survey_id uuid NOT NULL,
  period_start date NOT NULL,
  period_end date NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'active'::survey_status,
  sent_at timestamp with time zone,
  closed_at timestamp with time zone,
  total_recipients integer DEFAULT 0,
  total_responses integer DEFAULT 0,
  response_rate numeric,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT survey_instances_pkey PRIMARY KEY (id),
  CONSTRAINT survey_instances_survey_id_fkey FOREIGN KEY (survey_id) REFERENCES public.surveys(id)
);
CREATE TABLE public.survey_questions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  survey_id uuid NOT NULL,
  question_text text NOT NULL,
  help_text text,
  question_type USER-DEFINED NOT NULL DEFAULT 'rating'::survey_question_type,
  min_value integer DEFAULT 1,
  max_value integer DEFAULT 5,
  min_label character varying,
  max_label character varying,
  options jsonb,
  is_required boolean NOT NULL DEFAULT true,
  sort_order integer NOT NULL DEFAULT 0,
  category character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT survey_questions_pkey PRIMARY KEY (id),
  CONSTRAINT survey_questions_survey_id_fkey FOREIGN KEY (survey_id) REFERENCES public.surveys(id)
);
CREATE TABLE public.survey_responses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  survey_id uuid NOT NULL,
  instance_id uuid,
  team_member_id uuid,
  anonymous_token uuid DEFAULT gen_random_uuid(),
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  completed_at timestamp with time zone,
  is_complete boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT survey_responses_pkey PRIMARY KEY (id),
  CONSTRAINT survey_responses_survey_id_fkey FOREIGN KEY (survey_id) REFERENCES public.surveys(id),
  CONSTRAINT survey_responses_instance_id_fkey FOREIGN KEY (instance_id) REFERENCES public.survey_instances(id),
  CONSTRAINT survey_responses_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.surveys (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  survey_type character varying NOT NULL DEFAULT 'pulse'::character varying,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::survey_status,
  frequency USER-DEFINED NOT NULL DEFAULT 'once'::survey_frequency,
  start_date date,
  end_date date,
  next_send_date date,
  target_all_employees boolean NOT NULL DEFAULT true,
  target_team_ids ARRAY,
  target_team_member_ids ARRAY,
  is_anonymous boolean NOT NULL DEFAULT true,
  allow_comments boolean NOT NULL DEFAULT true,
  reminder_enabled boolean NOT NULL DEFAULT true,
  reminder_days_before_close integer DEFAULT 2,
  welcome_message text,
  thank_you_message text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT surveys_pkey PRIMARY KEY (id),
  CONSTRAINT surveys_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT surveys_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.talking_points (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  manager_team_member_id uuid NOT NULL,
  report_team_member_id uuid NOT NULL,
  added_by_team_member_id uuid,
  title character varying NOT NULL,
  notes text,
  category character varying,
  is_recurring boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  last_discussed_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT talking_points_pkey PRIMARY KEY (id),
  CONSTRAINT talking_points_manager_team_member_id_fkey FOREIGN KEY (manager_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT talking_points_report_team_member_id_fkey FOREIGN KEY (report_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT talking_points_added_by_team_member_id_fkey FOREIGN KEY (added_by_team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.target_measurables (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  target_id uuid NOT NULL,
  measurable_type character varying NOT NULL,
  measurable_id uuid NOT NULL,
  aggregation_type character varying NOT NULL DEFAULT 'latest'::character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT target_measurables_pkey PRIMARY KEY (id),
  CONSTRAINT target_measurables_target_id_fkey FOREIGN KEY (target_id) REFERENCES public.targets(id)
);
CREATE TABLE public.targets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  goal_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  target_value numeric NOT NULL,
  current_value numeric NOT NULL DEFAULT 0,
  starting_value numeric NOT NULL DEFAULT 0,
  unit character varying,
  weight numeric NOT NULL DEFAULT 1.0 CHECK (weight > 0::numeric),
  status USER-DEFINED NOT NULL DEFAULT 'not_started'::goal_status,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  CONSTRAINT targets_pkey PRIMARY KEY (id),
  CONSTRAINT targets_goal_id_fkey FOREIGN KEY (goal_id) REFERENCES public.goals(id)
);
CREATE TABLE public.task_collection_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  collection_id uuid NOT NULL,
  task_id uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  sort_order integer NOT NULL DEFAULT 0,
  organization_id uuid,
  CONSTRAINT task_collection_items_pkey PRIMARY KEY (id),
  CONSTRAINT task_collection_items_collection_id_fkey FOREIGN KEY (collection_id) REFERENCES public.task_collections(id),
  CONSTRAINT task_collection_items_task_id_fkey FOREIGN KEY (task_id) REFERENCES public.tasks(id),
  CONSTRAINT fk_task_collection_items_org FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.task_collections (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  query_config jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT task_collections_pkey PRIMARY KEY (id),
  CONSTRAINT task_collections_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  owner_team_member_id uuid,
  created_by_user_id uuid NOT NULL,
  parent_task_id uuid,
  project_id uuid,
  goal_id uuid,
  meeting_id uuid,
  title character varying NOT NULL,
  description text,
  status USER-DEFINED NOT NULL DEFAULT 'not_started'::task_status,
  priority USER-DEFINED NOT NULL DEFAULT 'medium'::task_priority,
  due_date timestamp with time zone,
  completed_at timestamp with time zone,
  sort_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  source_agenda_item_id uuid,
  source_meeting_id uuid,
  notes text,
  CONSTRAINT tasks_pkey PRIMARY KEY (id),
  CONSTRAINT tasks_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT tasks_owner_team_member_id_fkey FOREIGN KEY (owner_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT tasks_created_by_user_id_fkey FOREIGN KEY (created_by_user_id) REFERENCES public.users(id),
  CONSTRAINT tasks_parent_task_id_fkey FOREIGN KEY (parent_task_id) REFERENCES public.tasks(id),
  CONSTRAINT tasks_project_id_fkey FOREIGN KEY (project_id) REFERENCES public.projects(id),
  CONSTRAINT tasks_goal_id_fkey FOREIGN KEY (goal_id) REFERENCES public.goals(id),
  CONSTRAINT tasks_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id),
  CONSTRAINT fk_tasks_meeting FOREIGN KEY (meeting_id) REFERENCES public.meetings(id)
);
CREATE TABLE public.team_member_snapshots (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  snapshot_date date NOT NULL,
  period_type USER-DEFINED NOT NULL DEFAULT 'weekly'::snapshot_period,
  period_start date NOT NULL,
  period_end date NOT NULL,
  goals_total integer DEFAULT 0,
  goals_on_track integer DEFAULT 0,
  goals_at_risk integer DEFAULT 0,
  goals_completed integer DEFAULT 0,
  goal_progress_avg numeric,
  tasks_total integer DEFAULT 0,
  tasks_completed integer DEFAULT 0,
  tasks_overdue integer DEFAULT 0,
  task_completion_rate numeric,
  one_on_ones_held integer DEFAULT 0,
  one_on_ones_scheduled integer DEFAULT 0,
  meetings_attended integer DEFAULT 0,
  feedback_given integer DEFAULT 0,
  feedback_received integer DEFAULT 0,
  recognition_given integer DEFAULT 0,
  recognition_received integer DEFAULT 0,
  engagement_score numeric,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT team_member_snapshots_pkey PRIMARY KEY (id),
  CONSTRAINT team_member_snapshots_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT team_member_snapshots_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.team_members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  manager_user_id uuid,
  linked_user_id uuid,
  first_name character varying NOT NULL,
  last_name character varying NOT NULL,
  nickname character varying,
  email character varying,
  phone character varying,
  job_title character varying,
  department character varying,
  hire_date date,
  birthday date,
  location character varying,
  bio text,
  avatar_url text,
  linkedin_url character varying,
  employment_status USER-DEFINED NOT NULL DEFAULT 'active'::employment_status,
  termination_date date,
  is_active boolean NOT NULL DEFAULT true,
  active_goal_count integer NOT NULL DEFAULT 0,
  open_task_count integer NOT NULL DEFAULT 0,
  last_meeting_date timestamp with time zone,
  next_meeting_date timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  sync_id uuid DEFAULT gen_random_uuid(),
  sync_version integer DEFAULT 1,
  sync_modified_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'synced'::sync_status,
  CONSTRAINT team_members_pkey PRIMARY KEY (id),
  CONSTRAINT team_members_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT team_members_manager_user_id_fkey FOREIGN KEY (manager_user_id) REFERENCES public.users(id),
  CONSTRAINT team_members_linked_user_id_fkey FOREIGN KEY (linked_user_id) REFERENCES public.users(id),
  CONSTRAINT team_members_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT team_members_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.team_memberships (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  team_id uuid NOT NULL,
  team_member_id uuid NOT NULL,
  is_lead boolean NOT NULL DEFAULT false,
  joined_at timestamp with time zone NOT NULL DEFAULT now(),
  left_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  CONSTRAINT team_memberships_pkey PRIMARY KEY (id),
  CONSTRAINT team_memberships_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.teams(id),
  CONSTRAINT team_memberships_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT team_memberships_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.team_snapshots (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  team_id uuid NOT NULL,
  snapshot_date date NOT NULL,
  period_type USER-DEFINED NOT NULL DEFAULT 'weekly'::snapshot_period,
  period_start date NOT NULL,
  period_end date NOT NULL,
  member_count integer DEFAULT 0,
  active_member_count integer DEFAULT 0,
  goals_total integer DEFAULT 0,
  goals_on_track integer DEFAULT 0,
  goals_completed integer DEFAULT 0,
  goal_completion_rate numeric,
  tasks_total integer DEFAULT 0,
  tasks_completed integer DEFAULT 0,
  task_completion_rate numeric,
  one_on_ones_completion_rate numeric,
  team_meetings_held integer DEFAULT 0,
  avg_engagement_score numeric,
  survey_response_rate numeric,
  feedback_exchanges integer DEFAULT 0,
  recognition_count integer DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT team_snapshots_pkey PRIMARY KEY (id),
  CONSTRAINT team_snapshots_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT team_snapshots_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.teams(id)
);
CREATE TABLE public.teams (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  name character varying NOT NULL,
  description text,
  color character varying,
  lead_user_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  CONSTRAINT teams_pkey PRIMARY KEY (id),
  CONSTRAINT teams_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT teams_lead_user_id_fkey FOREIGN KEY (lead_user_id) REFERENCES public.users(id),
  CONSTRAINT teams_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT teams_deleted_by_fkey FOREIGN KEY (deleted_by) REFERENCES public.users(id)
);
CREATE TABLE public.user_roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  organization_id uuid NOT NULL,
  role_id uuid NOT NULL,
  team_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  assigned_by uuid,
  CONSTRAINT user_roles_pkey PRIMARY KEY (id),
  CONSTRAINT fk_user_roles_team FOREIGN KEY (team_id) REFERENCES public.teams(id),
  CONSTRAINT user_roles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT user_roles_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT user_roles_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.roles(id),
  CONSTRAINT user_roles_assigned_by_fkey FOREIGN KEY (assigned_by) REFERENCES public.users(id)
);
CREATE TABLE public.user_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  device_id text NOT NULL,
  device_name text,
  device_type character varying NOT NULL,
  os_name character varying,
  app_version character varying,
  refresh_token_hash text,
  last_active_at timestamp with time zone NOT NULL DEFAULT now(),
  last_ip_address inet,
  is_active boolean NOT NULL DEFAULT true,
  revoked_at timestamp with time zone,
  revoked_reason text,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT user_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  supabase_auth_id uuid UNIQUE,
  organization_id uuid,
  email character varying NOT NULL,
  display_name character varying NOT NULL,
  first_name character varying,
  last_name character varying,
  avatar_url text,
  phone character varying,
  timezone character varying DEFAULT 'UTC'::character varying,
  linked_team_member_id uuid,
  preferences jsonb DEFAULT '{}'::jsonb,
  notification_settings jsonb DEFAULT '{"goal_updates": true, "feedback_received": true, "meeting_reminders": true, "email_notifications": true}'::jsonb,
  is_active boolean NOT NULL DEFAULT true,
  is_email_verified boolean NOT NULL DEFAULT false,
  last_login_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by character varying,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by character varying,
  job_title character varying,
  company character varying,
  firm_id uuid,
  username character varying,
  is_admin boolean NOT NULL DEFAULT false,
  role character varying NOT NULL DEFAULT 'manager'::character varying,
  password_hash text,
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT fk_users_linked_team_member FOREIGN KEY (linked_team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT users_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);
CREATE TABLE public.vector_embeddings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  entity_type character varying NOT NULL,
  entity_id uuid NOT NULL,
  content_hash character varying NOT NULL,
  content_preview character varying,
  embedding USER-DEFINED,
  metadata jsonb,
  model_name character varying NOT NULL DEFAULT 'text-embedding-ada-002'::character varying,
  model_version character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  chunk_index integer NOT NULL DEFAULT 0,
  content text,
  embedding_dimensions integer NOT NULL DEFAULT 1536,
  is_deleted boolean NOT NULL DEFAULT false,
  deleted_at timestamp with time zone,
  deleted_by uuid,
  CONSTRAINT vector_embeddings_pkey PRIMARY KEY (id),
  CONSTRAINT vector_embeddings_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id)
);